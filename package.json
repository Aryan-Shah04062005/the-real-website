{
  "name": "the-real-root",
  "version": "1.0.0",
  "description": "Root package runner to ensure fail-proof cloud deployments",
  "scripts": {
    "build": "cd server && npm install",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
